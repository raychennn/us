# 1. 建置輔助 (優先安裝，幫助 pip 正確識別環境)
wheel
setuptools

# 2. 機器人核心
python-telegram-bot[job-queue]
APScheduler
python-dotenv
pytz

# 3. 關鍵限制 (絕對不能拿掉！)
# 只要鎖住 numpy 不要升上 2.0，其他套件讓 pip 自動去配對最穩定的版本
numpy<2.0.0

# 4. 資料科學套件 (不鎖版本)
# pip 會自動尋找支援上述 numpy 版本且有 "Binary Wheel" (免編譯) 的最新 pandas
pandas
pandas_ta
yfinance
requests
